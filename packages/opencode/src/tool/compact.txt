Placeholder compaction tool. Accepts message ID ranges to signal what should eventually be archived or summarized, but performs no compaction and returns a "Not yet implemented" notice. Usage: call with `ranges: [{ startMessageId: "msg_abc", endMessageId?: "msg_xyz" }]`; ranges are inclusive and may be a single message when `endMessageId` is omitted. No storage or message changes occur in this stub.
