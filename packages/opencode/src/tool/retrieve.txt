Placeholder retrieval tool. Accepts an archiveId (the first message ID of an archived range) and returns a "Not yet implemented" response without reading or restoring data. Usage: call with `{ archiveId: "msg_abc" }` typically when you see a `[SMART_ARCHIVED: ...]` placeholder. This stub performs no retrieval and makes no storage changes.
